include: ../analysis_options.yaml

linter:
  rules:
    # Integration / e2e tests emit TAP-style pass/fail markers via print();
    # that is intentional test output, not stray logging.
    avoid_print: false
